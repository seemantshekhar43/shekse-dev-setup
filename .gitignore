# herdr runtime artifacts (regenerated at runtime; never commit)
dotfiles/home/.config/herdr/*.log
dotfiles/home/.config/herdr/session.json
dotfiles/home/.config/herdr/*.sock

# macOS + nix build noise
.DS_Store
result
result-*

# no-mistakes local validation state - never commit in this public repo
.no-mistakes/

# Claude Code local/private state
.claude/

# Local-only Claude settings (machine-specific; skills are still tracked)
dotfiles/home/.claude/settings.json
